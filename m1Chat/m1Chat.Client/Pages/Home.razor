@page "/"
@inject NavigationManager NavigationManager

@code {
    protected override void OnInitialized()
    {
        // Redirect to the /Chat page when the root URL is accessed
        NavigationManager.NavigateTo("/Chat", forceLoad: false);
    }
}